---
const { slideNumber, imageSrc, slideBack, slideAhead } = Astro.props;
---

<div id={slideNumber} class="carousel-item relative w-full">
  <img src={imageSrc} class="w-full rounded-xl" />
  <div class="absolute flex justify-between transform -translate-y-1/2 left-5 right-5 top-1/2">
    <a href={slideBack} class="btn btn-circle">
      ❮
    </a>
    <a href={slideAhead} class="btn btn-circle">
      ❯
    </a>
  </div>
</div>