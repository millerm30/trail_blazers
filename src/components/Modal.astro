---
const { headingText, paragraphText, photoName, imgSource, imgAlt, modalId } = Astro.props;
---

<input type="checkbox" id={modalId} class="modal-toggle" />
<div class="modal">
  <div class="modal-box relative max-w-5xl">
    <label for={modalId} class="btn btn-sm btn-circle absolute right-2 top-2 border-none hover:bg-[#FB8500]">✕</label>
    <h3 class="text-lg font-bold">{headingText}</h3>
    <p class="py-2 italic">{paragraphText}</p>
    <p class="py-2 italic">{photoName}</p>
    <img class="w-full" src={imgSource} alt={imgAlt} />
  </div>
</div>