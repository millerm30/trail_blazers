---
import { Icon } from "astro-icon";

const { socialHref, socialType } = Astro.props;
---

<a href={socialHref} target="_blank"><Icon class="w-8 inline mx-1 hover:text-gray-700" name={socialType} /></a>