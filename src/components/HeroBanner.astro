---
const { backgroundImage, heroText } = Astro.props;
---

<div class="flex flex-col justify-center items-center h-full" id="heroImage">
	<img src={backgroundImage} alt="Hero Image" class="w-full h-72 opacity-40 object-fill object-center" />
	<h2 class="text-3xl md:text-4xl lg:text-4xl absolute text-gray-500">{heroText}</h2>
</div>