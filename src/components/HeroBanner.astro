---
const { backgroundImage, heroText } = Astro.props;
---

<div class="flex flex-col justify-center items-center h-full relative" id="heroImage">
	<img src={backgroundImage} alt="Hero Image" class="w-full h-72 opacity-80 object-cover object-center" />
	<h2 class="text-3xl md:text-4xl lg:text-4xl absolute right-0 bottom-0 text-gray-900 bg-[#f4f4f4] p-2 rounded-tl-lg opacity-50">{heroText}</h2>
</div>